# NetScan (Technical)

NetScan is implemented as a Python network scanner focused on fast and flexible host discovery.

- Python 3 for core logic
- socket-based TCP probe and banner grab
- asynchronous scanning routines for faster coverage
- network packet timing heuristics for reliable detection
- extensible host report output
